import { ScramjetContext } from "@/shared";
import { URLMeta } from "@rewriters/url";
export declare function rewriteWorkers(context: ScramjetContext, js: string | Uint8Array, isModule: boolean, url: string, meta: URLMeta): string;
